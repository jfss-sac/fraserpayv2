import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sell",
};

export default function SellPage() {
  return <h1 className="text-2xl font-bold text-foreground">Sell</h1>;
}
