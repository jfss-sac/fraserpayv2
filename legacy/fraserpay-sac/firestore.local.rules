// Emulator-only rules for the legacy reference app.
// This file must never be deployed to a real Firebase project.
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
